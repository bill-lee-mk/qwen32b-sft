# 复制为 .env 并填入真实值，或直接 export 到 shell
# 请勿提交 .env 到 git

# === 生成模型 API（必填）===
OPENROUTER_API_KEY=

# === 评估 API（InceptBench，必填）===
INCEPTBENCH_API_KEY=

# === 备用评估（可选，主配置失败时自动降级）===
# EVALUATOR_TOKEN=
